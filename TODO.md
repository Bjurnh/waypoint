- [x] Implement dark-mode switching for `PrayerLogScreen` while preserving original light design

- [ ] Update `PrayerLogScreen` to use Theme-derived colors (colorScheme) instead of light-only constants where needed
- [ ] Ensure `GradientBackground.prayer` decorative elements stay hidden in dark mode (already handled)
- [ ] Run `flutter analyze` / `flutter test` (or at least `flutter analyze`) to confirm no build errors

