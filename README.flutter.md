This folder contains a minimal Flutter app scaffold converted from the React app.

To run:

1. Install Flutter SDK: https://flutter.dev
2. From this folder run:

```bash
flutter pub get
flutter run
```

Files added:
- lib/main.dart
- lib/screens/*
- lib/widgets/*
- pubspec.yaml

Next steps:
- Expand screens to match original app functionality
- Add state management, persistence, and assets as needed
